# react-lesson
ドットインストールのreact入門

# git
```
cd workspace
git clone https://github.com/kujira-f/react-lesson.git
cd react-lesson
git branch test
git status

git add .
git diff
git commit -m "test"
git push origin master
```
